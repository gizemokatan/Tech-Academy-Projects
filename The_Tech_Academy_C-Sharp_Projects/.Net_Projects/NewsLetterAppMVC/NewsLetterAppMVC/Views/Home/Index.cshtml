
@*We are using Bootstrap*@
@*No spcae between columns and margin all around of about 1.5em*@
<div class="row no-gutters m-3">
    @*On a small, medium, and large screen, these are the number of columns we want.
    And we center the margin on the x axis setting it to auto. 
    And add a shadow to te card.*@
    <div class="card col-sm-12 col-med-10 col-lg-6 mx-auto shadow">
        <div class="card-header text-center">
            <h5 class="m-0">Sign up to our newsletter</h5>
        </div>
        <div class="card-body">
            @*Razor has a syntax for creating forms.
            This is an Html helper tag that generates html.
            Submitting info to the server so we are using Post*@
            @using (Html.BeginForm("SignUp", "Home", FormMethod.Post))
            {
            <div class="row no-gutters">
                <div class="form-group col-md-12">
                    <input name="FirstName" type="text" class="form-control" placeholder="First Name" />
                </div>
                <div class="form-group col-md-12">
                    <input name="LastName" type="text" class="form-control" placeholder="Last Name" />
                </div>
                <div class="form-group col-md-12">
                    <input name="EmailAddress" type="email" class="form-control" placeholder="Email Address" />
                </div>
                <button type="submit" class="btn btn-block btn-primary">Submit</button>
            </div>
            }

        </div>
    </div>
</div>